services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: stock_manager
    ports:
      - '5432:5432'
    volumes:
      - stock-manager-postgres:/var/lib/postgresql/data

volumes:
  stock-manager-postgres:
